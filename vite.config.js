import { defineConfig } from "vite"

export default defineConfig({
  // Sets the base path to relative so that if you deploy this to GitHub Pages or Netlify, the CSS and JS links will accurately point to the right locations.
  base: './',
  plugins: []
})